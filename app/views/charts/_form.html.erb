<%= form_with(model: chart, local: true) do |form| %>
  <% if chart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chart.errors.count, "error") %> prohibited this chart from being saved:</h2>

      <ul>
        <% chart.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :symptoms %>
    <%= form.text_area :symptoms %>
  </div>

  <div class="field">
    <%= form.label :treatment %>
    <%= form.text_area :treatment %>
  </div>

  <div class="field">
    <%= form.label :date_attendance %>
    <%= form.date_select :date_attendance %>
  </div>

  <div class="field">
    <%= form.label :pet_id %>
    <%= form.text_field :pet_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
